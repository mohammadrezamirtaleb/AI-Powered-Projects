# Place pre-trained model weights here.
#
# Download the SSD ResNet face detector model by running:
#   python scripts/download_models.py
#
# Files that will be downloaded:
#   - deploy.prototxt
#   - res10_300x300_ssd_iter_140000.caffemodel
#
# These files are gitignored (they are large binary files).
# Do NOT commit model weights to git — use download_models.py instead.
